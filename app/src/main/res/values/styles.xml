<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/background</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="colorControlNormal">#fff</item>
        <item name="colorControlActivated">#fff</item>
        <item name="colorControlHighlight">#fff</item>
    </style>

    <style name="SignUp" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/background</item>
        <item name="colorAccent">#000</item>
    </style>

    <style name="Pengajuan" parent="Theme.AppCompat.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/background</item>
        <item name="colorAccent">#444</item>
        <item name="colorControlNormal">#989898</item>
        <item name="colorControlActivated">#989898</item>
        <item name="colorControlHighlight">#989898</item>
        <item name="android:spinnerDropDownItemStyle">@style/CustomSpinner</item>
    </style>

    <style name="MyTextInputLayout" parent="Pengajuan">
        <item name="colorAccent">#444</item>
    </style>

    <style name="CustomSpinner" parent="Widget.AppCompat.Light.DropDownItem.Spinner">
        <item name="android:textColor">#444</item>
        <item name="android:colorBackground">#000</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="AlertDialogCustom" parent="@style/Base.Theme.AppCompat.Light.Dialog.MinWidth">
        <item name="android:textColor">@color/white</item>
        <item name="android:windowNoTitle">true</item>
    </style>

</resources>
